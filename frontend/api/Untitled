# TASK: IDENTIFY DEAD CODE AND UNUSED DEPENDENCIES

I have migrated from Azure OpenAI to OpenAI (gpt-4o) and Supabase.
I need to clean up my `frontend/api` folder.

**My Entry Points are:**
1. `frontend/api/index.py` (Vercel Handler)
2. `frontend/api/app/main.py` (FastAPI App)

**Please analyze the code and generate a REPORT of:**

1.  **Unused Python Files:**
    - List any `.py` files in `frontend/api` that are NEVER imported (directly or indirectly) starting from the entry points.
    - Specifically look for files named like `azure_*.py`, `config_old.py`, or similar.

2.  **Unused Libraries in `requirements.txt`:**
    - Check `frontend/api/requirements.txt`.
    - Compare it with the actual `import` statements in my code.
    - List libraries that are installed but NOT used in the code (e.g., `azure-identity`, `langchain`, `tiktoken` if not used).

3.  **Unused Functions/Variables:**
    - Inside `main.py` and `routers`, are there any commented-out code blocks or functions that are never called?

**DO NOT DELETE ANYTHING YET.** Just list them for me to verify.