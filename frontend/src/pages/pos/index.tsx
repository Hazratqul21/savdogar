"use client";

import { AdaptivePosLayout } from "@/components/pos/AdaptivePosLayout";

/**
 * Adaptive POS Page (Pages Router Version)
 * 
 * This file is for Pages Router structure (if using Next.js Pages Router).
 * For App Router, use: app/dashboard/pos-adaptive/page.tsx
 * 
 * To use this file, you would need to:
 * 1. Move it to a 'pages' directory at the root level
 * 2. Configure Next.js to use Pages Router
 * 3. Or use this structure if migrating to a different framework
 */
export default function PosPage() {
  return <AdaptivePosLayout />;
}




